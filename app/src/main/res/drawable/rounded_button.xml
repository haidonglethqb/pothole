<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="#1F41BB" /> <!-- Thay đổi màu nếu cần -->

    <!-- Bo góc -->
    <corners android:radius="16dp" />

    <!-- Viền (nếu cần) -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF" /> <!-- Thay đổi màu viền nếu cần -->

</shape>
